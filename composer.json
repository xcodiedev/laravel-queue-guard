{
    "name": "xcodiedev/laravel-queue-guard",
    "description": "Development-time guard that inspects a queued job before it is dispatched and warns about payloads that will fail, corrupt, or bloat the queue.",
    "keywords": ["laravel", "queue", "jobs", "serialization", "sqs", "developer-tools", "safety"],
    "license": "MIT",
    "type": "library",
    "authors": [
        {
            "name": "xcodiedev"
        }
    ],
    "require": {
        "php": "^8.1",
        "illuminate/contracts": "^10.0 || ^11.0 || ^12.0",
        "illuminate/support": "^10.0 || ^11.0 || ^12.0",
        "psr/log": "^1.1 || ^2.0 || ^3.0"
    },
    "require-dev": {
        "orchestra/testbench": "^8.0 || ^9.0 || ^10.0",
        "phpunit/phpunit": "^10.5 || ^11.0",
        "larastan/larastan": "^2.9 || ^3.0",
        "laravel/pint": "^1.18"
    },
    "autoload": {
        "psr-4": {
            "Xcodiedev\\QueueGuard\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Xcodiedev\\QueueGuard\\Tests\\": "tests/"
        },
        "classmap": [
            "tests/Fixtures/"
        ]
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse --memory-limit=1G",
        "format": "pint"
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "phpstan/extension-installer": true
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Xcodiedev\\QueueGuard\\QueueGuardServiceProvider"
            ]
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
